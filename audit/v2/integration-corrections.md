# Final-gate corrections

The first complete batch run returned 301 passed, 6 failed and 2 incomplete. Its diagnostic record is preserved in `initial-suite-results.txt`. One incomplete test was the desktop-only direct-input control test. The final run uses the desktop and records all real counts in `full-suite-results.txt`.

- V1.10, V1.11 and V1.12 inventory checks were using the evolving application directory. They now exclude the newly introduced V2 confirmed-list parser, which did not exist in those frozen releases.
- The V1.13 complete-audit tests used current V2 source for the old menu and inventory. They now execute functions extracted from the frozen, hash-checked V1.13 MLX. The archive's original report, findings, screenshots and fingerprint are preserved. Its old unfinished UI still has blank charts and a disabled calculator; the historical test checks that data remains saveable, while the V2 tests require the corrected status screen. A MATLAB on/off enumeration comparison was also corrected without changing behavior.
- The reachable-list test still proves that final bounds select exactly 0.10 mm. Its instruction assertion now requires a nonempty instruction, because V2's list explanation no longer repeats the number already shown prominently as the requested gap.
- The capture cleanup now targets only `Neyer gap test - inputs`. A regression created an unrelated figure and observed it being deleted before the fix; the corrected capture preserves it. The capture is invoked in a separate workspace so its `onCleanup` variable cannot overwrite the test's own cleanup.
- Matrix generation, membership and boundary checks were added first as failing tests. Checkpoint initialization initially rejected a differently shaped structure; the one-study write/resume test now proves the resulting checkpoint is unchanged on replay. The completeness gate rejects missing and duplicated IDs.
- Final CSV collection exposed MATLAB inferring an all-empty stop-reason column as numeric in one shard and text in another. A failing regression reproduced the mismatch; the collector now combines only its required numeric certification columns. The full suite was rerun with that regression included.
- The lower Help screenshots exposed 13 paragraph fragments where three complete paragraphs were intended. The last confidence and qualification warnings were clipped. A regression observed 13 paragraphs and a broken sentence; explicit string grouping restored three complete paragraphs. No words, math or numerical settings changed. Help captures now allow asynchronous scrolling to settle and include the final bottom view. A new confirmed-list input screenshot complements the regular input view.

After that Help correction, the Live Script was rebuilt once, then the entire suite and both complete matrices were repeated against the final source. Final evidence uses `matrices`; the earlier successful matrices remain separately labeled `pre-final-matrices`. The final report and reproducibility instructions rely on tracked files, not ignored MATLAB diaries.

Git attributes preserve the sealed generated source and evidence bytes. The original V1.13 manifest was checked again after its archival commit, and its recorded hashes still match. The archive's two intentional Markdown hard line breaks were preserved rather than altering the frozen visual record.
